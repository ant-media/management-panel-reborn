# Loaded by `pnpm dev-live` (vite --mode live). Talks to a real Ant Media Server.
# Backend defaults to http://localhost:5080; override per run:
#   VITE_BACKEND=http://host:port pnpm dev-live
VITE_USE_MOCKS=false
